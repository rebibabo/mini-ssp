# 配置模板 —— 复制为 .env 并填入你自己的真实值：
#   cp .env.example .env
# .env 不会被提交（.gitignore 已忽略），.env.example 会提交，供他人参照。
DB_USERNAME=root
DB_PASSWORD=your_mysql_password
# 可选：覆盖默认数据库连接
# DB_URL=jdbc:mysql://localhost:3306/mini_ssp?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
